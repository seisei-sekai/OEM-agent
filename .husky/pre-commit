#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run DDD snapshot generation
echo "📸 Generating DDD snapshot..."
pnpm ddd:snapshot

# Generate DDD diff diagrams
echo "📊 Generating DDD diff diagrams..."
tsx scripts/hooks/pre-commit-ddd-diff.ts

# Run lint-staged
pnpm lint-staged
